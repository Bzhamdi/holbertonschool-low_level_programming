#include "holberton.h"
/**
 **_memcpy - cpy adr src to dest
 *@n: param
 *@dest:param
 *@src:param
 *Return: dest
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
unsigned int i;
for (i = 0; i <= n; i++)
dset[i] = src[i];
return (dest);
}
