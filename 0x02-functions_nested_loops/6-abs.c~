#include <stdlib.h>
#include <stdio.h>
#include "holberton.h"
/**
 * _abs - check the number inserted
 * @n: number recieved (param)
 *
 *Return: abc
 */
int _abs(int n)
{
int a = n;
if (a > 0)
{
return (a);
}
else 
return (-a);
}
